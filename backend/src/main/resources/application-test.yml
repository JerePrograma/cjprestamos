spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cjprestamos_test
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration
  security:
    user:
      name: operadora_test
      password: operadora_test_123
